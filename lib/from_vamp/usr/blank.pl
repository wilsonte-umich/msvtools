#!/usr/bin/perl
use strict;
use warnings;

use vars(qw(%param %types %fields %refSeqs %reverseRefSeqs %fieldNames));

sub blank{
	print "jumpstarting job queue";
	sleep;
    #do nothing
}

1;

